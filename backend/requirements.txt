# ---- Web / API ----
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12.0
python-multipart>=0.0.9

# ---- Config / validation ----
pydantic>=2.6
pydantic-settings>=2.2

# ---- DB ----
SQLAlchemy>=2.0
# psycopg[binary]>=3.1   # uncomment for PostgreSQL; SQLite is the local-dev default

# ---- Scheduler ----
APScheduler>=3.10

# ---- Agents (LLM) ----
anthropic>=0.40
google-genai>=1.0          # Gemini provider (optional; selectable from the UI)

# ---- Brokers / data ----
alpaca-py>=0.21          # M2  US stocks + crypto (paper default)
ccxt>=4.2                # M7  crypto exchanges
oandapyV20>=0.7          # M7  forex + metals
MetaTrader5>=5.0; sys_platform == "win32"   # Exness / MT5 brokers (Windows only)

# ---- HTTP ----
httpx>=0.27

# ---- Dev / test ----
pytest>=8.0
pytest-asyncio>=0.23
